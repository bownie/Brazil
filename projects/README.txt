This is the projects directory for Friendlier app deployments.  An app will be initially copied from a template
to the project directory, but then the app will be build within the project directory.

At least that is the plan.
